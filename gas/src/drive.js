/**
 * Google ドライブ操作（FR-PDF-001 / FR-PDF-003 / FR-NTF-002）。
 *
 * 入力・処理済み・エラーの 3 フォルダ分離を維持する。
 * TODO: listInputFolderPdfFiles_ / moveFileToProcessedFolder_ / moveFileToErrorFolder_ を実装。
 */

/**
 * インプットフォルダ内の PDF ファイル一覧を返す。
 * @returns {GoogleAppsScript.Drive.File[]}
 */
function listInputFolderPdfFiles_() {
  throw new Error('未実装: listInputFolderPdfFiles_（drive.js）');
}
