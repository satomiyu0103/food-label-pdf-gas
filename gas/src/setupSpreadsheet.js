/**
 * 商品DB シートの作成・ヘッダー初期化（FR-SHT-001）。
 *
 * TODO: setupCreateDataSheetInActive / applyHeadersToDataSheet_ を商品DB向けに実装。
 */

/**
 * 開いているスプレッドシートに商品DB タブを作成し、ヘッダーを設定する。
 */
function setupCreateDataSheetInActive() {
  throw new Error('未実装: setupCreateDataSheetInActive（setupSpreadsheet.js）');
}
