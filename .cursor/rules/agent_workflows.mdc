---
description: （後方互換）agent_core + agent_implement へ移行済み。常時適用しない。
alwaysApply: false
---

# agent_workflows（後方互換スタブ）

このファイルは **スリム化のため分割** されました。常時適用はされません。

| 用途 | 正本 |
|---|---|
| 禁止事項・CHANGELOG 最低義務 | [`.cursor/rules/agent_core.mdc`](agent_core.mdc)（`alwaysApply: true`） |
| Phase 1〜3 要約（`src/`・`doc/` 等） | [`.cursor/rules/agent_implement.mdc`](agent_implement.mdc) |
| Phase 3 詳細・`doc/records/` 分割 | [`doc/ai_guidelines/agent_phase3_dod.md`](../../doc/ai_guidelines/agent_phase3_dod.md) |

旧リンクから辿った場合は上記3つを参照してください。
