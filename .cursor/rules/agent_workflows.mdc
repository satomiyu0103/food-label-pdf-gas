---
description: （後方互換）agent_core + agent_implement_entry へ移行済み。常時適用しない。
alwaysApply: false
---

# agent_workflows（後方互換スタブ）

このファイルは **スリム化のため分割** されました。常時適用はされません。

| 用途 | 正本 |
|---|---|
| 禁止事項・CHANGELOG 最低義務 | [`.cursor/rules/agent_core.mdc`](agent_core.mdc)（`alwaysApply: true`） |
| 実装タスク入口（`src/`・`doc/` 等） | [`.cursor/rules/agent_implement_entry.mdc`](agent_implement_entry.mdc) |
| Phase 1〜3 手順 | `.cursor/skills/implementation-phase1/` 〜 `phase3-doc-updates/` |
| Phase 3 詳細・`doc/records/` 分割 | [`.cursor/skills/phase3-doc-updates/SKILL.md`](../skills/phase3-doc-updates/SKILL.md) |
| 開発ルール（命名・Git・テスト等） | `.cursor/rules/` 各 `*.mdc` |

旧リンクから辿った場合は上記を参照してください。
